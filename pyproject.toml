[project]
name = "fishsense-api-workflow-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "temporalio>=1.15.0",
]
